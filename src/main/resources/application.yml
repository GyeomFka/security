server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/securitydb
    driver-class-name: org.mariadb.jdbc.Driver
    username: securitydbuser
    password: 920214

  jpa:
    hibernate:
      ddl-auto: create #update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
