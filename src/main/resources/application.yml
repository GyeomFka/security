server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/securitydb
    driver-class-name: org.mariadb.jdbc.Driver
    username: securitydbuser
    password: 920214
  profiles:
    include: google
  jpa:
    hibernate:
      ddl-auto: update #create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          google: # /oauth2/authorization/google 이 주소를 동작하게 한다.
            client-id: 997520953217-tbl0knfudndcdhv9h7ge2ghakft5buct.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile
